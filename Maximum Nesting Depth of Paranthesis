class Solution:
    def maxDepth(self, s):
        current_depth = 0
        max_depth = 0
        for i in s:
            if i == "(":
                current_depth += 1
                if max_depth < current_depth:
                    max_depth = current_depth
            if i == ")":
                current_depth -= 1
        return(max_depth)
